<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      * {
        margin: 0;
        box-sizing: border-box;
      }

      /* Background */
      
      #background {
        display: flex;
        flex-direction: column;
        flex-grow: 1;
        background-color: rgb(37, 47, 74);
        min-height: 100vh;
      }

      /* Form */

      #container-login {
        border: 1px solid white;
        width: 350px;
        height: 285px;
        text-align: center;
        margin: auto;
        background-color: white;
        border-radius: 10px;
      }

      #container-register {
        border: 1px solid white;
        width: 350px;
        height: 350px;
        text-align: center;
        margin: auto;
        background-color: white;
        border-radius: 10px;
      }

      /* Form text header */

      #from-text {
        color: black;
        margin-top: 20px;
        margin-bottom: 20px;
      }

      /* Input username */

      .username {
        position: relative;
        margin-bottom: 30px;
      }
      #from-input-username {
        border: 0.5px solid rgb(157, 157, 157);
        border-radius: 4px;
        width: 250px;
        height: 35px;
        padding: 0 15px;
        transition: 0.25s;
      }
      #from-label-username {
        color: rgba(0, 0, 0, 0.572);
        position: absolute;
        top: 50%;
        transform: translateY(-50%);
        left: 55px;
        user-select: none;
        pointer-events: none;
        transition: 0.25s;
      }
      #from-input-username:not(:placeholder-shown) + #from-label-username,
      #from-input-username:focus + #from-label-username {
        top: 0;
        left: 55px;
        padding: 0px 7px 0px 7px;
        display: inline-block;
        background-color: white;
        color: black;
        transition: 0.25s;
      }

      /* Input passowrd */

      .password {
        position: relative;
        margin-bottom: 30px;
      }
      #from-input-password {
        border: 0.5px solid rgb(157, 157, 157);
        border-radius: 4px;
        width: 250px;
        height: 35px;
        padding: 0 15px;
        transition: 0.25s;
      }
      #from-label-password {
        color: rgba(0, 0, 0, 0.572);
        position: absolute;
        top: 50%;
        transform: translateY(-50%);
        left: 55px;
        user-select: none;
        pointer-events: none;
        transition: 0.25s;
      }
      #from-input-password:not(:placeholder-shown) + #from-label-password,
      #from-input-password:focus + #from-label-password {
        top: 0;
        left: 55px;
        padding: 0px 7px 0px 7px;
        display: inline-block;
        background-color: white;
        color: black;
        transition: 0.25s;
      }

      /* Comfirm Passowrd */

      .comfirmpassword {
        position: relative;
        margin-bottom: 30px;
      }
      #from-input-comfirmpassword {
        border: 0.5px solid rgb(157, 157, 157);
        border-radius: 4px;
        width: 250px;
        height: 35px;
        padding: 0 15px;
        transition: 0.25s;
      }
      #from-label-comfirmpassword {
        color: rgba(0, 0, 0, 0.572);
        position: absolute;
        top: 50%;
        transform: translateY(-50%);
        left: 55px;
        user-select: none;
        pointer-events: none;
        transition: 0.25s;
      }
      #from-input-comfirmpassword:not(:placeholder-shown)
        + #from-label-comfirmpassword,
      #from-input-comfirmpassword:focus + #from-label-comfirmpassword {
        top: 0;
        left: 55px;
        padding: 0px 7px 0px 7px;
        display: inline-block;
        background-color: white;
        color: black;
        transition: 0.25s;
      }

      /* Submit */

      #form-button {
        font-size: 20px;
        width: 250px;
        height: 35px;
        background-color: rgb(0, 123, 255);
        color: white;
        border: 0.5px solid;
        cursor: pointer;
        transition: 0.25s;
      }
      #form-button:hover {
        background-color: hsl(211, 100%, 60%);
      }

      /* Form text link */

      #text-link {
        margin-top: 10px;
      }
      #text-link a {
        text-decoration: none;
        transition: 0.25s;
        color: rgb(0, 132, 255);
      }
      #text-link a:hover {
        text-decoration: underline;
        transition: 0.25s;
      }
    </style>
    <title>Login</title>
  </head>
  <body>
    <div id="background">
      <div id="container-login">
        <h1 id="from-text">Login</h1>
        <form action="/login" method="post">
          <div class="username">
            <input
              type="text"
              id="from-input-username"
              placeholder=" "
              name="username"
            />
            <label for="from-label-username" id="from-label-username"
              >Username</label
            >
          </div>
          <div class="password">
            <input
              type="password"
              id="from-input-password"
              placeholder=" "
              name="password"
            />
            <label for="from-label-password" id="from-label-password"
              >Password</label
            >
          </div>
          <button id="form-button" name="submitlogin" type="submit">
            LOGIN
          </button>
        </form>
        <p id="text-link">
          Bạn chưa có tài khoản? <a href="/register">Đăng ký</a>
        </p>
      </div>
    </div>
  </body>
</html>
